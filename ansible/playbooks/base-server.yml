---
# Base configuration - run on all managed servers
# Usage: ansible-playbook playbooks/base-server.yml

- name: Apply base configuration to all servers
  hosts: all
  become: yes
  roles:
    - base